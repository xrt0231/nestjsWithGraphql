# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Franchise {
  id: String!
  category: String!
}

type Store {
  id: String!
  name: String!
  size: Int!
  area: String
  franchise: Franchise
}

type Query {
  stores: [Store!]!
  store(id: ID!): Store!
}

type Mutation {
  createStore(input: StoreCreateInput!): Store!
  updateStore(input: StoreUpdateInput!, id: ID!): Store!
  deleteStore(id: ID!): [Store!]!
  createFranchise(data: FranchiseInput!): Franchise!
}

input StoreCreateInput {
  name: String!
  size: Int!

  """地區"""
  area: String
  franchise: ID!
}

input StoreUpdateInput {
  name: String
  size: Int

  """地區"""
  area: String
  franchise: ID!
}

input FranchiseInput {
  category: String!
}
